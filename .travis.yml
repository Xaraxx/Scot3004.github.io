language: ruby
rvm:
- 2.1
node_js:
- '6'

before_install:
- export TZ=America/Bogota
- node --version
- nvm --version

install:
  - gem install bundler
  - bundle install
  - npm install

script:
 - jekyll build
 - scss-lint ./_sass
 - htmlproofer ./_site --disable-external

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

cache:
  bundler: true
  directories:
    - bower_components
    - node_modules

sudo: false
