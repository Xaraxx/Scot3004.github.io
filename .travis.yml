language: python
python: 2.7
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/lektor/builds
install:
    - "pip install Lektor"
before_script:
    - npm install -g bower
    - bower install
script: "lektor build && lektor deploy ghpages"
